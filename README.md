# DreamndGame2

## The idea of this branch is to make the code cross platorm and OS agnostic. ( First step)

## Second requirenment is to refactor the engine a little bit so the client side game goes not end up being an Inheritance of the Application() class. 